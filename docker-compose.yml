services:
  glasswing:
    build: .
    image: glasswing:latest
    ports:
      - "8080:8080"
    environment:
      VNC_PASSWORD: "change-me-now"
      WIDTH: "1600"
      HEIGHT: "1000"
      DEPTH: "24"
